export const updateChildName = (text) => ({
  type: 'UPDATE_CHILD_NAME',
  text,
});

export const updateChildId = (text) => ({
  type: 'UPDATE_CHILD_ID',
  text,
});

export const updateParentName = (text) => ({
  type: 'UPDATE_PARENT_NAME',
  text,
});

export const updateParentId = (text) => ({
  type: 'UPDATE_PARENT_ID',
  text,
});
